# Creating-an-HTML-Form-and-PHP-Based-Database-Connection
This project uses PHP to connect to databases and develops an HTML login form that collects user data. The data is then stored in the database.
